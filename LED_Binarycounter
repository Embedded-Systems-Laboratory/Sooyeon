#define F_CPU 16000000UL
#include <avr/io.h>
#include <util/delay.h>

int main(void)
{
    /* Binary counter */
	unsigned char count=0; //unsigned char: 8bit
	DDRA=0xff;
	
    while (1) 
    {
		PORTA=count;
		_delay_ms(500);
		
		count++;
    }
}
