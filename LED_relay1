#define F_CPU 16000000UL
#include <avr/io.h>
#include <util/delay.h>

int main(void)
{
    /* LED relay1 */
	DDRA=0xff;

	
    while (1) 
    {
		for (int i=0; i<8; i++)
		{
			PORTA= 0x01 << i;
			_delay_ms(1000);
			PORTA=0x00;
			_delay_ms(1000);
		}
    }
}
