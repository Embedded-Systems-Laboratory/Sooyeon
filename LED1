#define F_CPU 16000000UL
#include <avr/io.h>
#include <util/delay.h>

int main(void)
{
	// 포트 방향 설정
	DDRA = 0xFF;  // 릴레이/LED 제어용 (출력)
	PORTA = 0x00;

	/* 8개 LED 순차 점등 프로그램 */
	while (1)
	{
		for(int i=0; i<8; i++)
		{
			PORTA=(0x01 <<i);
			_delay_ms(100);
		}
	}
	return 0;
}
